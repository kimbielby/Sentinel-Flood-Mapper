[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel_flood_mapper"
version = "0.1.0"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["sentinel_flood_mapper*"]